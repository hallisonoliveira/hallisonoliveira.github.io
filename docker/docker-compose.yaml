version: "3"

services:
  jekyll:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ../:/srv/jekyll
      - ../vendor/bundle:/usr/local/bundle  # cache de gems
    ports:
      - "4000:4000"
    environment:
      - JEKYLL_ENV=development
